import { TransferForm } from "./TransferForm";

export default {
  title: "Compositions/TransferForm",
  component: TransferForm,
  parameters: {
    docs: {
      description: {
        component:
          "A fake transfer form assembled from Input and Button. It takes no props and does not send real money.",
      },
    },
  },
};

export const Default = {};
